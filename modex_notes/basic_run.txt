conda activate /data2/sserbin/conda_envs/olmt
module load hdf5/1.12.1-gcc850 netcdf/4.8.1-gcc850

python3.9 site_fullrun.py \
--site US-LL3 --sitegroup SoutheastUS --caseidprefix SoutheastUS --nyears_ad_spinup 200 \
--nyears_final_spinup 400 --tstep 1 --cpl_bypass --spinup_vars --compiler gnu --mpilib mpi-serial \
--gswp3 --model_root /data/sserbin/Modeling/FASSt-simulation/E3SM \
--caseroot /data2/Model_Output/olmt_seus_ens --ccsm_input /data/Model_Data/cesm_input_datasets \
--runroot /data2/Model_Output/olmt_seus_ens --no_submit --machine modex

# submit the ad spinup
sbatch --cpus-per-task=1 scripts/SoutheastUS/ad_spinup_group0.pbs
# NOTE: run gets submitted but then runs as a backgound process, by design

# adjust restarts

# submit final spinup

# NOTE: run gets submitted but then runs as a backgound process, by design

# submit transient run

# NOTE: run gets submitted but then runs as a backgound process, by design
